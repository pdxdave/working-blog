import React from 'react'
import {ContentWraper} from '../elements'

export const ContentWraper = ({children}) => {
    return <ContentWraper>
                {children}
            </ContentWraper>
}